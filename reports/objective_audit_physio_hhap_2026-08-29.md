# Objective audit — 29 August physiology / HHAP write

**Mode:** Decide. Not a Gate 3 freeze. Not a Model 2/3 health fit.

Three inherit auditors scored the 29 August Discussion insert against the shorter 14-page Hogan stop (Guo/Goggins neighbours, then mortality baselines). Named Max seats were not used; they fail to start when Cloud Agent usage is exhausted.

## Verdicts

| Seat | Overall | Would keep the long insert? |
|---|---|---|
| Fable-role (claim–evidence, kill-list) | **WORSE** | Delete |
| Opus-role (Hogan spare English) | **WORSE** | No; Table A2 is the better WHO vehicle |
| Sol-role (identification / policy routing) | **MIXED** | Mapping is the right genre; this instance paired residuals with alerts |

**Parent synthesis:** the long insert was **worse than the shorter Discussion** as published science. It kept the refusals but licensed a mechanism tour (n=7 confinement; stacked afterload; WHO catalogue) and “can inform” language that a skimmer can read as support for Prolonged Heat. Kill-list items were not hit. Bishai’s ask still stands; the honest vehicle is a short hypothesis plus an instruments-only map.

## What this session did with that verdict

1. Cut the live Discussion to short hypothesis paragraphs. Removed “can inform four,” “sits with official hot-night counts,” “continued attention,” and the Ioannou 26.3 °C / +0.2 °C stack.
2. Laidlaw Table A2 no longer puts Model 1 residuals in the “can inform” cells.
3. Did **not** freeze Gate 3. Did **not** invent Model 2/3 health coefficients. The governed panels are absent.

## What cannot be done in this checkout

- Fit Model 2 or Model 3 on real CHD/HF counts (`data_processed/chd_analysis_panel.csv` and `hf_analysis_panel.csv` missing).
- Reconstruct 132 monthly counts from Figure 1 or annual totals.
- Close Gate 3 as confirmatory (all twelve *q* > 0.19).
- Close Gate 3 as a team freeze (needs Hogan, Roro, Bishai, and Bob).
- Close stroke Gate 1/2, Hogan weather lock, or IRB.

Plumbing now in place for a real fit after drop-in: `CONTROL_TERM_MAP` accepts `relative_humidity` and `rainfall`; `scripts/68_hogan_model3_climatology.R` exists; synthetic Model 2/3 fits pass in `tests/test_final_model_helpers.R`.
