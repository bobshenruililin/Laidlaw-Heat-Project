# Sol 5.6 critique of the compounding search harness — 24 August 2026

**Seat:** Sol (`gpt-5.6-sol-xhigh`). Hostile methods read of this extract’s search system. Not a theory of science.

## Spare

- K1. All core *q* > 0.19 is a refusal-solution, not harness failure.
- K2. Hogan / Roro / Bishai / Bob holes are human-owned. Not harness failure.
- K3. Constitution bounds originality; no general theory of science.
- Estimand mixing: AMI/stroke missing-fields vs excess-death class already split (L09).

## Apply (landed in harness this cycle)

- Gameable width/depth/proxy → unique-family width; proxy is never a target.
- `kill_with: null` allowed → killed nodes must carry `table_contradiction` or `forbidden_claim_hit` plus evidence.
- Human gate relabelled `alive/agent_owned` → `human_owned` must stay `blocked_human`; shrinking that set needs `owner_event`.
- Failed cycle overwrites the no-reset JSON → write `cycle_latest.json` only when ok.
- Daily-lag overstatement → do **not** paste into Hogan’s live file; L02 now distinguishes dimensional non-identification of an unconstrained daily DLNM from failed constrained calibration *here*.

## Apply (named gaps; not built this cycle)

- Event log of attempts/duration (solving *rate*, not inventory).
- Execute `eval_rubric.yml` as a typed claim parser (today it is documentation + duplicated constants).
- Hash-check Stage 3 PDFs and the full Hogan weather block (today: start-string tripwire + rails string).
- Semantic estimand parser (forbidden: `estimand_policy` remains a string tripwire).

## Rails audit

Gate 3 remains open. Hogan weather start string required. Stage 3 lock not rebuilt. No invented stroke/daily/monetisation coefficients.
