# Objective audit

Execute skill `objective-audit`.

1. Read `.cursor/skills/objective-audit/SKILL.md`.
2. Run `python3 scripts/71_objective_audit_tripwires.py`.
3. Inherit auditors only unless named Max seats actually start.
4. WORSE is allowed. Do not freeze Gate 3. Do not invent Model 2/3 coefficients.
