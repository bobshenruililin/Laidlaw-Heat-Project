# Health-econ lab note — 10 September 2026

**Playbook 08.** Explore. SYNTHETIC. Gate 3 open. Live thermal file not edited by this lab.

## What ran

`python3 scripts/72_synthetic_utilisation_shock.py`  
`python3 scripts/73_synthetic_mnar_selection.py` (night-shift mutation: MNAR missingness and selected-sample truncation)

A constant synthetic cold-day coefficient plus an unobserved utilisation drop after month 84 can make a **full-window** count ratio look smaller than a pre-2020 fit. MNAR missingness and truncation of high post-2020 counts are further wrecking operations on the **same** DGP. That is a demonstration that Hogan’s “improved” CR can be a **utilisation / selection artefact** on fake data. It is not evidence about Hong Kong.

Output: `outputs/health_econ/synthetic_utilisation_shock_2026-09-10.json` and `synthetic_mnar_selection_2026-09-10.json` (`data_status: SYNTHETIC`).

## Anti-convergence

Rejected this cycle (already killed or wrong extract): TWFE, QALY, cartel, time-inconsistency-as-paper, nudges, React-as-paper, labs-from-Tmin, health-econ night engine in the live file. Registry: `analysis_plan/covid_period/hypothesis_registry.yml`.

## Escalation taken (SYNTHETIC only)

GPT-5.6 Sol was quota-blocked for the live-file adversary. Grok ran MNAR + truncation on the synthetic DGP. No missing-data Monte Carlo on HA rows (DUA).

**Next mutation (do not treat the lab as finished):** influenza-collapse DGP (zero a synthetic flu term after month 84) versus utilisation. Still SYNTHETIC. Still not the journal draft.

## Journal track

Not copied into `manuscript/covid_period/Manuscript_covid_period_draft.md` as a number. The draft may cite this note as a method warning only, labelled SYNTHETIC.
